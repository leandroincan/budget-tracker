services:
  - type: web
    name: budget-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run budget_app.py --server.port=$PORT --server.address=0.0.0.0
